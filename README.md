# Teste prático React Native

Instale as dependências antes de executar o projeto. Para isto, no diretório raíz do projeto, execute "npm install" via terminal, ou "yarn", caso não utilize por padrão o npm.
